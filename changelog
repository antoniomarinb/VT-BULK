Ver 0.2:
\t Migrated from vt python packages, requests are now being made with python.requests
\t Programs that need scanning get placed in a queue, which gets retrieved at the end
\t .json dumps for every file scanned
\t Added some super cool ascii art
\t Program had an identity crysis, renamed twice.
\t Rebuilt most of the file sending logic
\t Ask the user for api_key and user_id if not found
\t Get API daily usage

Ver 0.3
\t Multi-threading is here!
\t Simplified program (I think)
\t Reworked whole program functionality
\t API minutely rate limit is now taken into account (requesting a new file scan it is).
